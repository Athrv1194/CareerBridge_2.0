package com.careerbridge.gateway.config;

// No Spring Security on classpath -- authentication is JwtAuthenticationFilter (servlet filter).
// Kept as a signpost so the absence reads as a decision, not an oversight.
public class SecurityConfig {
}
